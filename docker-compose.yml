version: "3.9"

services:

  backend:
    build: ./backend
    container_name: student-backend
    ports:
      - "5000:5000"

  frontend:
    build: ./frontend
    container_name: student-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend